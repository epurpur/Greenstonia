// import required models

const Area = require('./Area');
const Boulder = require('./Boulder');
const Route = require('./Route');
const User = require('./User')

// export models to be used by schema
module.exports = { Area, Boulder, Route, User };